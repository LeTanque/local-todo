# Copy to server/.env before running the API.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=todo
DB_USER=todo
DB_PASSWORD=todo
PORT=4311

# Optional client override (Next.js rewrites use this at build/dev time).
# API_URL=http://localhost:4311
